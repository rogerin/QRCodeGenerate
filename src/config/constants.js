const constants = {
    params: {
        ecl: {
            defaultValue: "L",
            options: ['L', 'M', 'H', 'Q']
        },
        background: {
            defaultValue: "white",
            options: { message: 'ALL HEXA exemple #FF0000 '}
        },
        color: {
            defaultValue: "#000000",
            options: { message: 'ALL HEXA exemple #FF0000 '}
        },
        width: {
            defaultValue: "200",
            options: { message: 'Numbem in pixel '}
        },
        height: {
            defaultValue: "200",
            options: { message: 'Numbem in pixel '}
        }
    }
}

module.exports = constants;